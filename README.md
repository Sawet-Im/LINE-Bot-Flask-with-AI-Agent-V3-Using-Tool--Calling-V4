# LINE Bot Flask with AI Agent V3 (SQL Tool+ RAG Tool)

โปรเจกต์นี้คือการต่อยอดจาก LINE Bot Flask with AI Agent V2 โดยเพิ่มความสามารถด้าน RAG (Retrieval-Augmented Generation) เข้าไปใน Agent ทำให้ Agent สามารถเข้าถึงแหล่งข้อมูลความรู้ทั่วไปหรือข้อมูลตัวอย่างคำถาม-คำตอบ และ ข้อมูลธุรกิจ (เมนู, โปรโมชั่น)

# คุณสมบัติใหม่หลัก (RAG)

* Agent: Agent สามารถ ตัดสินใจว่าจะใช้ RAG Tool (knowledge_base_search) เพื่อค้นหาข้อมูลทั่วไป (FAQ, นโยบาย) หรือใช้ SQL Tool สำหรับข้อมูลธุรกิจ (เมนู, โปรโมชั่น)
* Knowledge Base: ข้อมูลความรู้ถูกจัดเก็บเป็น Vector ใน ChromaDB (บน Disk) ทำให้ประหยัด Token Cost และลด Startup Time
* Separation of Concern: มีการ ซ่อนตาราง knowledge_base จาก SQL Toolkit โดยจงใจ เพื่อ บังคับ ให้ Agent ใช้ RAG Tool สำหรับคำถามความรู้ทั่วไปเท่านั้น (Critical Step)
* Efficiency & Cost Saving: ข้อมูลความรู้ถูกโหลดและอ่าน เฉพาะเมื่อ Agent ตัดสินใจ ว่าจำเป็นต้องใช้ RAG Tool เท่านั้น ซึ่งประหยัดค่าใช้จ่ายด้าน Token อย่างมากเมื่อเทียบกับการใส่ความรู้ทั้งหมดใน Prompt หลัก


# 1. โครงสร้างโปรเจกต์ (Updated)
```
มีการเพิ่มไฟล์และโฟลเดอร์สำหรับ RAG (Vector DB) เข้ามา:
├── templates/
│   ├── dashboard.html
│   └── setup.html
├── my_app/
│   ├── api_app.py
│   ├── admin_app.py
│   ├── ai_processor.py
│   ├── database.py                       # เพิ่มตาราง knowledge_base
│   ├── agent_setup.py                    
│   └── agent_setup_sql_agent_and_rag.py  # เพิ่ม RAG Logic, initialize_rag_retriever 
├── chroma_db_data/                       # 🟢 โฟลเดอร์ใหม่: สำหรับเก็บ Index ของ ChromaDB
├── .env                                  # เพิ่ม GOOGLE_API_KEY, CHROMA_DB_DIR
├── requirements.txt                      # เพิ่ม chromadb, langchain-google-genai
└── README.md
```

# 2. RAG ในฐานะเครื่องมือของ Agent

หัวใจของการพัฒนานี้คือการทำให้ Agent ตัดสินใจ เลือกใช้ Tool ที่เหมาะสมที่สุด โดยมีตรรกะการตัดสินใจดังนี้:
ประเภทคำถาม | Tool ที่ใช้ | แหล่งข้อมูล	| การจัดการใน LLM
ข้อมูลธุรกิจ (เมนู, ราคา, โปรโมชั่น) | SQL Tool	| ตาราง SQL (menu, promotions) | LLM สร้างคำสั่ง SQL และเรียกใช้
ข้อมูลทั่วไป/นโยบาย (บัตรเครดิต, เวลาทำการ, การคืนสินค้า) | RAG Tool(knowledge_base_search) | ตาราง knowledge_base(ผ่าน ChromaDB) | LLM ส่งคำถามไปยัง Tool, Tool ค้นหา, LLM สรุปคำตอบจากผลลัพธ์

###### Note on Efficiency: LLM จะอ่านข้อมูลความรู้จาก Vector Database (ChromaDB) เฉพาะตอนที่ Agent ตัดสินใจว่าต้องใช้ RAG Tool เท่านั้น ซึ่งประหยัดค่าใช้จ่ายและเวลาแฝง (Latency) มากกว่าการใส่ความรู้ทั้งหมดใน Prompt หลัก (In-Context Learning)


# 3. การติดตั้งและการตั้งค่า (Configuration)

## 3.1. การติดตั้ง Dependencies

นอกจากไลบรารีเดิมแล้ว ต้องติดตั้ง RAG Dependencies เพิ่มเติม:
```
pip install -r requirements.txt
```
## 3.2. การตั้งค่า API Key

สร้างไฟล์ชื่อ .env และเพิ่มค่าที่จำเป็น รวมถึงตัวแปรใหม่สำหรับ RAG:
```
GOOGLE_API_KEY="YOUR_GEMINI_API_KEY"    
# สำหรับการตอบกลับด้วย LINE API
LINE_CHANNEL_ACCESS_TOKEN="YOUR_LINE_ACCESS_TOKEN"
LINE_CHANNEL_SECRET="YOUR_LINE_CHANNEL_SECRET"
# ngrok url
BASE_URL="NGROK_URL"     
OLLAMA_HOST="OLLAMA_URL"
```

## 3.3. การเตรียมฐานข้อมูลและ RAG Indexing

1. สร้าง DB: รันฟังก์ชัน initialize_db() เพื่อสร้างตาราง knowledge_base เพิ่มใน agent_db.sqlite
2. Indexing: เมื่อรัน api_app.py ครั้งแรก ระบบจะเรียกใช้ฟังก์ชัน initialize_rag_retriever เพื่อ:
    * ดึงข้อมูลจากตาราง knowledge_base (Source of Truth)
    * แปลงข้อมูลเป็น Vector โดยใช้ GoogleGenerativeAIEmbeddings
    * บันทึก Vector Index ไว้ในโฟลเดอร์ chroma_db_data (Persistence)


# 4. ขั้นตอนการพัฒนา (Critical Implementation Steps)

### 4.1 โครงสร้างไฟล์และการเปลี่ยนแปลงที่จำเป็น

ไฟล์ | การเปลี่ยนแปลงหลัก | จุดประสงค์ |
--- | --- | --- |
**`database.py`** | เพิ่มตาราง **`knowledge_base`** | เป็นแหล่งข้อมูลความรู้หลัก (Source of Truth) ก่อนเข้า ChromaDB |
**`requirements.txt`** | เพิ่ม `chromadb`, `langchain-google-genai` | ติดตั้งไลบรารีที่จำเป็นสำหรับ RAG |
**`.env`** | เพิ่ม `GOOGLE_API_KEY` | กำหนด API Key สำหรับ Gemini Embeddings และ LLM |
**`agent_setup_sql_agent_and_rag.py`** | เพิ่ม RAG Logic, ซ่อนตาราง, ปรับ Prompt | กำหนด Tool, สร้าง Retriever และ **บังคับ Agent ให้ใช้ RAG** |

### 4.2 การเตรียม RAG Retriever (Indexing ด้วย ChromaDB)

ในฟังก์ชัน `initialize_rag_retriever` (ใน `agent_setup_sql_agent_and_rag.py`):

1. **ดึงข้อมูล (Load Data):** ดึงข้อมูลทั้งหมดจากตาราง **`knowledge_base`** ของ SQLite ที่ตรงกับ `store_id` (Source of Truth)
2. **Indexing:** แปลงข้อมูล (ข้อความ) ให้เป็น Vector โดยใช้ `GoogleGenerativeAIEmbeddings`
3. **Persistence:** ข้อมูล Vector ถูกบันทึกไว้ใน `CHROMA_DB_DIR` (บน Disk) เพื่อให้ **ไม่ต้อง Index ใหม่ทุกครั้ง**
4. **Creation:** สร้าง **`VectorStoreRetriever`** ที่พร้อมใช้งานเป็น Tool

> หมายเหตุ: ต้องมี Logic แยกต่างหากเพื่อ Sync (ซิงค์) ข้อมูลเมื่อมีการอัปเดต/เพิ่ม/ลบข้อมูลในตาราง knowledge_base ไปยัง ChromaDB
>

### 4.3 การสร้าง Tool และการซ่อนตาราง (Critical Step) 🛑
นี่คือขั้นตอนสำคัญที่ช่วยแก้ปัญหาการใช้ SQL Tool 
โค้ดที่ต้องทำ | จุดประสงค์ |
--- | --- |
**1. ซ่อนตาราง:** แก้ไขการตั้งค่า `SQLDatabase.from_uri` ใน `initialize_sql_agent`ให้ **ยกเว้นตาราง `knowledge_base`** จาก `include_tables` | บังคับให้ Agent **มองไม่เห็น** ตารางนี้ผ่าน SQL Toolkit เมื่อถามคำถาม RAG |
**2. สร้าง RAG Tool:** ใช้ `create_retrieval_tool` เพื่อแปลง Retriever ให้เป็น Tool ชื่อ **`knowledge_base_search`** | เตรียม Tool ให้ Agent สามารถเรียกใช้ได้ |
**3. รวม Tools:** รวม `sql_tools` และ `rag_tools_list` เข้าด้วยกันเพื่อสร้าง `final_tools`ที่ Agent จะใช้ | ให้ Agent มีทางเลือกทั้ง SQL และ RAG |

### 4.4 การปรับ Prompt (Agent Prefix) 📝

แก้ไข Prompt ในฟังก์ชัน `create_agent_prefix` เพื่อกำหนดกฎการตัดสินใจของ Agent อย่างชัดเจน:

1. **[การใช้ Tool ที่เหมาะสม]:** เพิ่มกฎที่ระบุชัดเจนว่าคำถามเกี่ยวกับนโยบาย, เวลาเปิด-ปิด, หรือข้อมูลที่ไม่ใช่ข้อมูลในตาราง SQL ให้ใช้ **`knowledge_base_search Tool`** เท่านั้น
2. **[การสร้างคำตอบจาก Tool]:** เพิ่มกฎบังคับให้ Agent ต้องใช้ข้อมูลในส่วน **"รายละเอียด:"** หรือ **"answer_or_detail"** จากผลลัพธ์ของ RAG Tool มาสร้างคำตอบ **ห้ามตอบด้วย Fallback Message** หาก Tool ส่งข้อมูลกลับมา

# 5. Flow การทำงานของ Agent (Action-Oriented Flow)

Flow นี้เป็นแบบ **Action-Oriented** (เน้นการดำเนินการ) ซึ่งมีความซับซ้อนกว่า RAG ธรรมดา (Knowledge-centric)

ขั้นตอนใน Agent Flow | หน้าที่ |
--- | --- |
**1. Agent Reasoning** | LLM (Gemini) อ่าน Query, อ่าน `AGENT_PREFIX` และ Memory เพื่อ **สร้างแผน (Plan)** และตัดสินใจ Intent (หาข้อมูลธุรกิจ หรือ หาความรู้) |
**2. Tool Selection** | Agent ตัดสินใจว่าจะใช้ **RAG Tool** หรือ **SQL Tool** |
**3. Retrieval (เฉพาะ RAG)** | หากเลือก RAG Tool: Tool ทำงานโดย แปลงคำถามเป็น Vector → ค้นหาใน Vector DB (ChromaDB) → ดึง **ข้อความที่เกี่ยวข้อง (Context)** ออกมา |
**4. Generation** | Agent นำ Context ที่ได้ (จาก RAG Tool หรือผลลัพธ์จาก SQL Tool) มา **ผนวก** เข้าไปใน Prompt อีกครั้ง และสั่งให้ LLM สรุปคำตอบโดยอ้างอิงจาก Context นั้นเท่านั้น |
**5. Final Answer** | Agent ส่งคำตอบที่สรุปแล้วกลับไปให้ลูกค้า |


# 6. ปัญหาที่ต้องแก้ไข (Troubleshooting Checklist)

### 6.1 ปัญหา AsyncIO/Threading

เมื่อใช้ **ChromaDB** และ **Gemini Embeddings** ใน Thread ที่ทำงานแบบ Synchronous อาจเกิดข้อผิดพลาด: **`Error: No current event loop`**

**วิธีแก้ไข:** คุณต้องติดตั้ง `nest_asyncio` และเพิ่มคำสั่งนี้ที่ส่วนบนสุดของไฟล์ **`agent_setup.py`** ก่อนการเรียกใช้ฟังก์ชันใด ๆ:

```python
import nest_asyncio
nest_asyncio.apply()
```

# 7. ประโยชน์หลักของการใช้ RAG Tool ร่วมกับ SQL Agent

การผสาน RAG ในรูปแบบ Tool เข้ากับ SQL Agent ไม่เพียงแต่ช่วยเพิ่มความรู้ให้กับ Agent เท่านั้น แต่ยังนำมาซึ่งข้อดีทางสถาปัตยกรรมและการดำเนินงานที่สำคัญ โดยเน้นที่การ **ตัดสินใจ** และ **ประสิทธิภาพ** ของ LLM

### 7.1 ข้อดีด้านประสิทธิภาพและต้นทุน (Efficiency & Cost)

ข้อดี | รายละเอียด | ผลกระทบต่อระบบ
--- | --- | --- |
**ลด Token Cost** | LLM อ่านข้อมูลความรู้ (Vector DB) **เฉพาะเมื่อ Agent ตัดสินใจ** ว่าจำเป็นต้องใช้ RAG Tool เท่านั้น | ประหยัดค่าใช้จ่ายด้าน Token อย่างมาก เมื่อเทียบกับการใส่ความรู้ทั้งหมดใน `AGENT_PREFIX` (In-Context Learning)
**เพิ่มความเร็ว (Latency)** | Agent สามารถ **ข้าม** กระบวนการ RAG ที่มีค่าใช้จ่ายสูงและใช้เวลามาก หากคำถามนั้นจัดการได้ดีกว่าด้วย SQL Tool | ลดเวลาแฝง (High Latency) โดยเฉพาะสำหรับคำถามเกี่ยวกับข้อมูลธุรกิจที่มีโครงสร้าง
**ความยืดหยุ่นของข้อมูล** | ข้อมูลความรู้ (นโยบาย, FAQ) สามารถถูก **อัปเดต/แก้ไขได้ง่าย** ใน Vector DB โดยไม่ต้องแก้ไขหรือ Redeploy Prompt หลักของ Agent | ง่ายต่อการบำรุงรักษาและการจัดการความรู้ (Knowledge Management)


## 8. ตัวอย่างคำถามและคำตอบ (Scenario-Based Examples)

ตัวอย่างนี้แสดงให้เห็นว่า Agent ใช้การตัดสินใจและเลือก Tool ที่ถูกต้องอย่างไรเมื่อต้องตอบคำถามที่หลากหลาย

สถานการณ์ (Query) | Agent Reasoning และ Tool Selection | LLM Output (Final Answer)
--- | --- | --- 
**ลูกค้า:** "เมนูวันนี้มีอะไรบ้าง ราคาเท่าไหร่?" | **Intent:** หาข้อมูลธุรกิจ **Tool Used:** **SQL Tool**Agent สร้าง SQL Command เพื่อดึงข้อมูลจากตาราง `menu` | "เมนูพิเศษวันนี้คือลาเต้ถั่วเหลือง ราคา 120 บาท และเค้กแครอท ราคา 85 บาทครับ"
**ลูกค้า:** "ร้านรับบัตรเครดิตหรือไม่ และมีที่จอดรถไหม?" | **Intent:** หาข้อมูลความรู้/นโยบาย **Tool Used:RAG Tool** (`knowledge_base_search`) Agent ค้นหาใน ChromaDB (knowledge_base) | "ทางร้านรับบัตรเครดิตทุกชนิด และมีที่จอดรถรองรับได้ 15 คัน บริเวณด้านหลังร้านครับ"
**ลูกค้า:** "สินค้าที่ซื้อไปสามารถคืนได้ภายในกี่วัน?" | **Intent:** หาข้อมูลนโยบาย **Tool Used:** **RAG Tool**(`knowledge_base_search`) Agent ค้นหานโยบายการคืนสินค้าใน ChromaDB | "ตามนโยบายการคืนสินค้า สินค้าสามารถคืนได้ภายใน 7 วัน นับจากวันที่ซื้อ โดยต้องมีใบเสร็จและไม่ผ่านการใช้งานครับ"
**ลูกค้า:** "เปลี่ยนโปรโมชั่นส่วนลดที่ผมเคยได้รับได้ไหม?" | **Intent:** หาข้อมูลธุรกิจ **Tool Used:** **SQL Tool**Agent สร้าง SQL Command เพื่อตรวจสอบตาราง `promotions` | "ขออภัยครับ โปรโมชั่นส่วนลดที่ท่านได้รับไม่สามารถเปลี่ยนแปลงได้ในขณะนี้ เนื่องจากถูกผูกกับรหัสส่วนลดของท่านแล้ว"

> สรุป: Agent ใช้ SQL Tool เพื่อโต้ตอบกับข้อมูลที่เปลี่ยนแปลงบ่อยและมีโครงสร้าง (เช่น สินค้าคงคลัง, ราคา) และใช้ RAG Tool เพื่อโต้ตอบกับความรู้ทั่วไปที่ค่อนข้างคงที่และไม่มีโครงสร้าง (เช่น นโยบาย, FAQ)
>
