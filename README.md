# LINE Bot Flask with AI Agent V3 (SQL Tool+ RAG Tool)

โปรเจกต์นี้คือการต่อยอดจาก LINE Bot Flask with AI Agent V2 โดยเพิ่มความสามารถด้าน RAG (Retrieval-Augmented Generation) เข้าไปใน Agent ทำให้ Agent สามารถเข้าถึงแหล่งข้อมูลความรู้ทั่วไปหรือข้อมูลตัวอย่างคำถาม-คำตอบ และ ข้อมูลธุรกิจ (เมนู, โปรโมชั่น)

# คุณสมบัติใหม่หลัก (RAG)

* Agent: Agent สามารถ ตัดสินใจว่าจะใช้ RAG Tool (knowledge_base_search) เพื่อค้นหาข้อมูลทั่วไป (FAQ, นโยบาย) หรือใช้ SQL Tool สำหรับข้อมูลธุรกิจ (เมนู, โปรโมชั่น)
* Knowledge Base: ข้อมูลความรู้ถูกจัดเก็บเป็น Vector ใน ChromaDB (บน Disk) ทำให้ประหยัด Token Cost และลด Startup Time
* Separation of Concern: มีการ ซ่อนตาราง knowledge_base จาก SQL Toolkit โดยจงใจ เพื่อ บังคับ ให้ Agent ใช้ RAG Tool สำหรับคำถามความรู้ทั่วไปเท่านั้น (Critical Step)
* Efficiency & Cost Saving: ข้อมูลความรู้ถูกโหลดและอ่าน เฉพาะเมื่อ Agent ตัดสินใจ ว่าจำเป็นต้องใช้ RAG Tool เท่านั้น ซึ่งประหยัดค่าใช้จ่ายด้าน Token อย่างมากเมื่อเทียบกับการใส่ความรู้ทั้งหมดใน Prompt หลัก


# 1. โครงสร้างโปรเจกต์ (Updated)
```
มีการเพิ่มไฟล์และโฟลเดอร์สำหรับ RAG (Vector DB) เข้ามา:
├── templates/
│   ├── dashboard.html
│   └── setup.html
├── my_app/
│   ├── api_app.py
│   ├── admin_app.py
│   ├── ai_processor.py
│   ├── database.py       # เพิ่มตาราง knowledge_base
│   └── agent_setup.py    # เพิ่ม RAG Logic, initialize_rag_retriever
├── chroma_db_data/       # 🟢 โฟลเดอร์ใหม่: สำหรับเก็บ Index ของ ChromaDB
├── .env                  # เพิ่ม GOOGLE_API_KEY, CHROMA_DB_DIR
├── requirements.txt      # เพิ่ม chromadb, langchain-google-genai
└── README.md
```

# 2. RAG ในฐานะเครื่องมือของ Agent

หัวใจของการพัฒนานี้คือการทำให้ Agent ตัดสินใจ เลือกใช้ Tool ที่เหมาะสมที่สุด โดยมีตรรกะการตัดสินใจดังนี้:
ประเภทคำถาม | Tool ที่ใช้ | แหล่งข้อมูล	| การจัดการใน LLM
ข้อมูลธุรกิจ (เมนู, ราคา, โปรโมชั่น) | SQL Tool	| ตาราง SQL (menu, promotions) | LLM สร้างคำสั่ง SQL และเรียกใช้
ข้อมูลทั่วไป/นโยบาย (บัตรเครดิต, เวลาทำการ, การคืนสินค้า) | RAG Tool(knowledge_base_search) | ตาราง knowledge_base(ผ่าน ChromaDB) | LLM ส่งคำถามไปยัง Tool, Tool ค้นหา, LLM สรุปคำตอบจากผลลัพธ์

###### Note on Efficiency: LLM จะอ่านข้อมูลความรู้จาก Vector Database (ChromaDB) เฉพาะตอนที่ Agent ตัดสินใจว่าต้องใช้ RAG Tool เท่านั้น ซึ่งประหยัดค่าใช้จ่ายและเวลาแฝง (Latency) มากกว่าการใส่ความรู้ทั้งหมดใน Prompt หลัก (In-Context Learning)


# 3. การติดตั้งและการตั้งค่า (Configuration)

## 3.1. การติดตั้ง Dependencies

นอกจากไลบรารีเดิมแล้ว ต้องติดตั้ง RAG Dependencies เพิ่มเติม:
```
pip install -r requirements.txt
```
## 3.2. การตั้งค่า API Key

สร้างไฟล์ชื่อ .env และเพิ่มค่าที่จำเป็น รวมถึงตัวแปรใหม่สำหรับ RAG:
```
GOOGLE_API_KEY="YOUR_GEMINI_API_KEY"    
# สำหรับการตอบกลับด้วย LINE API
LINE_CHANNEL_ACCESS_TOKEN="YOUR_LINE_ACCESS_TOKEN"
LINE_CHANNEL_SECRET="YOUR_LINE_CHANNEL_SECRET"
# ngrok url
BASE_URL="NGROK_URL"     
OLLAMA_HOST="OLLAMA_URL"
```

## 3.3. การเตรียมฐานข้อมูลและ RAG Indexing

1. สร้าง DB: รันฟังก์ชัน initialize_db() เพื่อสร้างตาราง knowledge_base เพิ่มใน agent_db.sqlite
2. Indexing: เมื่อรัน api_app.py ครั้งแรก ระบบจะเรียกใช้ฟังก์ชัน initialize_rag_retriever เพื่อ:
    * ดึงข้อมูลจากตาราง knowledge_base (Source of Truth)
    * แปลงข้อมูลเป็น Vector โดยใช้ GoogleGenerativeAIEmbeddings
    * บันทึก Vector Index ไว้ในโฟลเดอร์ chroma_db_data (Persistence)


# 4. ขั้นตอนการพัฒนา (Critical Implementation Steps)

### 4.1 โครงสร้างไฟล์และการเปลี่ยนแปลงที่จำเป็น

ไฟล์ | การเปลี่ยนแปลงหลัก | จุดประสงค์ |
--- | --- | --- |
**`database.py`** | เพิ่มตาราง **`knowledge_base`** | เป็นแหล่งข้อมูลความรู้หลัก (Source of Truth) ก่อนเข้า ChromaDB |
**`requirements.txt`** | เพิ่ม `chromadb`, `langchain-google-genai` | ติดตั้งไลบรารีที่จำเป็นสำหรับ RAG |
**`.env`** | เพิ่ม `GOOGLE_API_KEY` | กำหนด API Key สำหรับ Gemini Embeddings และ LLM |
**`agent_setup.py`** | เพิ่ม RAG Logic, ซ่อนตาราง, ปรับ Prompt | กำหนด Tool, สร้าง Retriever และ **บังคับ Agent ให้ใช้ RAG** |

### 3.2 การเตรียม RAG Retriever (Indexing ด้วย ChromaDB)

ในฟังก์ชัน `initialize_rag_retriever` (ใน `agent_setup.py`):

1. **ดึงข้อมูล (Load Data):** ดึงข้อมูลทั้งหมดจากตาราง **`knowledge_base`** ของ SQLite ที่ตรงกับ `store_id` (Source of Truth)
2. **Indexing:** แปลงข้อมูล (ข้อความ) ให้เป็น Vector โดยใช้ `GoogleGenerativeAIEmbeddings`
3. **Persistence:** ข้อมูล Vector ถูกบันทึกไว้ใน `CHROMA_DB_DIR` (บน Disk) เพื่อให้ **ไม่ต้อง Index ใหม่ทุกครั้ง**
4. **Creation:** สร้าง **`VectorStoreRetriever`** ที่พร้อมใช้งานเป็น Tool

> หมายเหตุ: ต้องมี Logic แยกต่างหากเพื่อ Sync (ซิงค์) ข้อมูลเมื่อมีการอัปเดต/เพิ่ม/ลบข้อมูลในตาราง knowledge_base ไปยัง ChromaDB
>

## 4.1. การสร้าง Tool และการซ่อนตาราง 🛑

โค้ดที่ต้องทำ	จุดประสงค์
1. ซ่อนตาราง: แก้ไขการตั้งค่า SQLDatabase.from_uri ให้ ยกเว้น ตาราง knowledge_base จาก include_tables	บังคับให้ Agent มองไม่เห็น ตารางนี้ผ่าน SQL Toolkit เมื่อถามคำถาม RAG 
```
 #เลือกเฉพาะตารางที่ LLM ต้องใช้ SQL (เช่น menu, promotions, tasks, ingredients, stores)
        db_instance = SQLDatabase.from_uri(
            db_uri, 
            include_tables=["menu", "promotions", "ingredients", "stores", "tasks"] 
            # ⚠️ ไม่รวม "knowledge_base"
        )
```
2. สร้าง RAG Tool: ใช้ create_retrieval_tool เพื่อแปลง Retriever ให้เป็น Tool ชื่อ knowledge_base_search	เตรียม Tool ให้ Agent สามารถเรียกใช้ได้
3. รวม Tools: รวม sql_tools และ rag_tools_list เข้าด้วยกันเพื่อสร้าง final_tools	ให้ Agent มีทางเลือกทั้ง SQL และ RAG
Export to Sheets

## 4.2. การปรับ Prompt (Agent Prefix) 📝

Prompt ถูกแก้ไขเพื่อกำหนดกฎการใช้ Tool อย่างเคร่งครัด:
1. [การใช้ Tool ที่เหมาะสม]: บังคับให้คำถามเกี่ยวกับ นโยบาย/ที่อยู่/เวลาทำการ ใช้ knowledge_base_search Tool เท่านั้น
2. [การสร้างคำตอบจาก Tool]: บังคับให้ Agent ต้องใช้เนื้อหาในส่วน "รายละเอียด:" หรือ "answer_or_detail" จากผลลัพธ์ของ RAG Tool มาสร้างคำตอบ ห้ามตอบด้วย Fallback Message


5. Flow การทำงานของระบบ (System Data Flow)

การตัดสินใจของ Agent ถูกเพิ่มเข้ามาใน Step 5:
Step	ไฟล์ที่ทำงาน	รายละเอียดการทำงานและ Context
1-4	(เหมือนเดิม)	รับ Task, Load Context (Store ID, Chat History), Initialize Agent (Prompt, Memory, Tools)
5. Agent Invocation	ai_processor.py	Agent ประมวลผลข้อความ: Agent Reasoning -> Tool Selection (SQL หรือ RAG) -> Tool Execution
6. Tool Execution	Agent Tools -> SQLite/ChromaDB	SQL: Agent สร้าง SQL Command, รันกับ agent_db.sqlite RAG: Agent เรียก knowledge_base_search, ค้นหาใน ChromaDB
7-8	(เหมือนเดิม)	Generation: Agent สรุปคำตอบจากผลลัพธ์ของ Tool -> Respond (ส่งคำตอบกลับ LINE)


6. การแก้ไขปัญหาสำคัญ (Troubleshooting)

* Error: No current event loop: หากเกิดข้อผิดพลาดนี้ ให้ติดตั้ง nest_asyncio และเพิ่ม import nest_asyncio; nest_asyncio.apply() ที่ส่วนบนสุดของไฟล์ agent_setup.py เพื่อจัดการปัญหา AsyncIO/Threading ที่เกิดจาก ChromaDB/Gemini Embeddings.
* การอัปเดตข้อมูล RAG: เมื่ออัปเดตข้อมูลในตาราง knowledge_base แล้ว ต้อง ลบโฟลเดอร์ chroma_db_data และ รัน api_app.py ใหม่ เพื่อบังคับให้ระบบทำการ Index ข้อมูลใหม่ทั้งหมด.
# LINE-Bot-Flask-with-AI-Agent-V3-Using-SQL-Tool-AND-RAG-Tool
